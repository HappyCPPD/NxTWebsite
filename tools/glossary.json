[
  { "term": "TreeEnsembleClassifier", "def": "An ONNX operator that packs a random-forest / gradient-boosted-tree model into a single portable node." },
  { "term": "ONNX", "def": "Open Neural Network Exchange - a portable file format for trained ML models." },
  { "term": "OOB read", "def": "Out-of-bounds read: accessing memory outside an array's allocated range, usually via an unchecked index." },
  { "term": "IEEE-754", "def": "The standard binary format for floating-point numbers, used by almost every language's float/double type." },
  { "term": "SUID", "def": "Set-user-ID: a file permission bit that runs a program as its owner (often root) instead of the invoking user." },
  { "term": "chroot", "def": "A syscall that changes a process's apparent root directory, used to sandbox or jail it." },
  { "term": "segfault", "def": "Segmentation fault: a crash caused by accessing memory the process isn't allowed to touch." },
  { "term": "binary search", "def": "A search that halves the remaining range each step, needing only log2(N) probes to pin down a value." },
  { "term": "oracle", "def": "In an exploit, anything that answers a single yes/no question about hidden state, one query at a time." },
  { "term": "little-endian", "def": "A byte order where the least-significant byte is stored first." },
  { "term": "stack canary", "def": "A random value placed before a return address; if overwritten by a buffer overflow, the program detects it and aborts." },
  { "term": "TPM", "def": "Trusted Platform Module: a chip (or emulated component) that stores keys and attests to system state." },
  { "term": "ROP", "def": "Return-oriented programming: chaining existing code fragments (gadgets) via the stack to execute arbitrary logic without injecting new code." },
  { "term": "ASLR", "def": "Address Space Layout Randomization: randomizes memory addresses each run to make exploits harder to write reliably." },
  { "term": "MT19937", "def": "The standard Mersenne Twister PRNG: a 624-word state and a fixed output transform, fully predictable once enough output is seen." },
  { "term": "Mersenne Twister", "def": "A fast, long-period pseudo-random generator (Python's `random`), but not cryptographically secure." },
  { "term": "Z3", "def": "An SMT solver: give it symbolic variables and constraints and it finds a satisfying assignment or proves none exists." },
  { "term": "LLL", "def": "Lenstra-Lenstra-Lovasz reduction: finds short, nearly orthogonal vectors of an integer lattice in polynomial time." },
  { "term": "approximate GCD", "def": "Recovering a hidden common factor p from samples p*q_i + r_i with small noise r_i, typically via a lattice." },
  { "term": "haversine", "def": "A formula for the great-circle distance between two latitude/longitude points on a sphere." },
  { "term": "MMSI", "def": "Maritime Mobile Service Identity: a 9-digit number identifying a ship's radio and AIS transponder." },
  { "term": "acrostic", "def": "A hidden message spelled by the first letter or word of each line or sentence." },
  { "term": "Rich header", "def": "An undocumented, XOR-masked block in MSVC-linked PE files recording which toolchain built each object file - a de facto build fingerprint." },
  { "term": "COFF", "def": "Common Object File Format: the header structure Windows PE files build on, including the linker's TimeDateStamp." },
  { "term": "dlopen", "def": "A libc call that loads a shared object into the calling process at runtime and resolves its symbols." },
  { "term": "Fermat factorization", "def": "A method for factoring n = p*q that succeeds fast when p and q are close together, by searching for a square a^2 - n." },
  { "term": "HCCB", "def": "High Capacity Color Barcode: a Microsoft format encoding data as a grid of coloured triangles instead of black-and-white cells." },
  { "term": "GTS", "def": "Global Terminal Service: Nintendo's online Pokemon trading system for the DS era, built on WFC (Wi-Fi Connection)." },
  { "term": "SIGTRAP", "def": "A signal raised on a trap instruction (like int3); catching it lets a program treat its own breakpoints as a dispatch mechanism." },
  { "term": "int3", "def": "The x86 one-byte breakpoint instruction (0xCC); executing it raises SIGTRAP and hands control to a debugger or signal handler." },
  { "term": "FNV-1a", "def": "A simple, fast non-cryptographic hash: XOR each input byte into a running value, then multiply by a fixed prime." },
  { "term": "rdtsc", "def": "An x86 instruction reading the CPU's cycle counter; comparing deltas between calls is a common anti-debugging timing check." },
  { "term": "mprotect", "def": "A syscall that changes the read/write/execute permissions of a memory page, e.g. making code writable for self-modification." },
  { "term": "log poisoning", "def": "Injecting attacker-controlled code into a log file, then triggering the application to execute or interpret that log as code." },
  { "term": "blob (git)", "def": "Git's object type for raw file contents, addressed by the SHA-1 hash of its content - identical content always hashes the same." },
  { "term": "IEND chunk", "def": "The final chunk of a PNG file, marking the end of image data; anything appended after it is ignored by image viewers." }
]
